#include <stdio.h>
/**
 * main - Programm for Entry point
 * Return: Always return 0 when successful
 */

int main(void)
{
	int c;

	c = 0;
	while
		(c < 10) {
			putchar(c + '0');
			c++;
		}
	putchar('\n');
	return (0);
}
