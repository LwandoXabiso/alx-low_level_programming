#include <stdio.h>
/**
 * main - Program Entry point
 * Return: Always return 0 when successful
 */

int main(void)
{
	char c;

	c = 'z';
	while
		(c <= 'a') {
			putchar(c);
			c--;
		}
	putchar('\n');
	return (0);
}
